apiVersion: v1
kind: Service
metadata:
  name: my-app-service
spec:
  selector:
    app: my-app
  ports:
  - protocol: TCP
    port: 80         # Port on which the service will be accessible
    targetPort: 3000 # The container port where the app is running
  type: LoadBalancer
